from setuptools import setup

setup(name='pyduino',
      version='0.7',
      description='A library for comfortable, fast and modern control of Arduino boards using python',
      packages=['pyduino'],
      install_requires=['pyserial'],
      author_email='mikhailsmnv22@gmail.com',
      zip_safe=False)
